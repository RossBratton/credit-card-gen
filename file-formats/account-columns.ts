import { ColumnFormat } from "./formats";

const columns: ColumnFormat[] = [
    {
        title: 'Id',
        key: 'id',
        type: 'string',
        defaultValue: 'id'
    }
];

export default columns;